// {{ display_name }} MCP server — generated by mcpify. Do not hand-edit.

import { describe, expect, it } from 'vitest';

import { StubAuthStrategy } from '../../../src/auth/strategies/stub.js';

describe('StubAuthStrategy', () => {
  it('authenticates to empty credentials', async () => {
    const strategy = new StubAuthStrategy();
    await expect(strategy.authenticate({})).resolves.toEqual({});
  });

  it('always validates', () => {
    const strategy = new StubAuthStrategy();
    expect(strategy.validateCredentials({})).toBe(true);
  });
});
