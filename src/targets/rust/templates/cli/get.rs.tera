// {{ display_name }} MCP server — generated by mcpify. Do not hand-edit.

use {{ crate_name }}::data::store::open_store;
use {{ crate_name }}::tools::get_tool::get_operation;

pub async fn run(operation_id: &str) -> anyhow::Result<()> {
    let conn = open_store(std::path::Path::new("mcp_store.db"))?;
    let result = get_operation(&conn, operation_id)?;
    println!("{}", serde_json::to_string_pretty(&result)?);
    Ok(())
}
