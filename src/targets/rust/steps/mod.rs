//! Per-step generation logic for the Rust target, one submodule per
//! `McpServerTargetGenerator` method — mirrors
//! `targets::typescript::steps`. Empty until Story R2 (`bootstrap_project`)
//! adds its first submodule.
