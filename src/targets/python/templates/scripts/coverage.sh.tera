#!/usr/bin/env bash
# {{ display_name }} MCP server — generated by mcpify. Do not hand-edit.
set -euo pipefail
cd "$(dirname "$0")/.."

uv run pytest --cov={{ module_name }} --cov-report=html --cov-report=json --cov-report=term

echo
echo "Coverage written to:"
echo "  htmlcov/index.html (human-readable)"
echo "  coverage.json       (machine-readable)"
