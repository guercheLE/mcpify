"""{{ display_name }} MCP server — generated by mcpify. Do not hand-edit.

Thin `python main.py` convenience wrapper around the installed
`{{ project_name }}` console script (`pyproject.toml`'s
`[project.scripts]` entry, Story P2) — both invocations dispatch to the
same Typer app.
"""

from {{ module_name }}.cli import main

if __name__ == "__main__":
    main()
