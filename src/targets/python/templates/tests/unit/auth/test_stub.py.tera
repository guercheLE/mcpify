"""{{ display_name }} MCP server — generated by mcpify. Do not hand-edit."""

from __future__ import annotations

from {{ module_name }}.auth.strategies.stub import StubAuthStrategy


async def test_always_authenticates_with_empty_credentials() -> None:
    strategy = StubAuthStrategy()
    credentials = await strategy.authenticate({})
    assert credentials == {}


def test_always_validates() -> None:
    strategy = StubAuthStrategy()
    assert strategy.validate_credentials({})
