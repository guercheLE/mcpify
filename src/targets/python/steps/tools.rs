use anyhow::Result;

use crate::context::GeneratorContext;

/// Story P6: `search`/`get`/`call` tools, the `sqlite3`/`aiosqlite` +
/// `sqlite-vec` data-access module, the `httpx`-based API client, and the
/// embedding module. Stubbed in P1.
pub async fn generate_mcp_tools(_ctx: &GeneratorContext) -> Result<()> {
    Ok(())
}
